# Activates application-test.yaml. Kept as a separate, differently-named file so the main
# application.yaml still loads and supplies everything the test profile does not override.
spring.profiles.active=test
